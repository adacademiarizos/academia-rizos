[
  {
    "what": "InicializaciÃ³n del proyecto",
    "why": "Establecer la arquitectura AI-First para escalar a largo plazo",
    "where": "Todo el repositorio",
    "learned": "El sistema usarÃ¡ Skills Registry, Context.md y Ramas Aisladas (feature) para evitar el colapso del contexto y la rotura de producciÃ³n."
  },
  {
    "what": "Recuperacion de contrasena con invalidacion de sesiones JWT",
    "why": "Permitir que cuentas con credenciales recuperen acceso sin exponer enumeracion de usuarios ni dejar sesiones antiguas activas",
    "where": "Auth, Prisma, mail transaccional y pantallas publicas",
    "learned": "Con NextAuth en estrategia JWT, invalidar sesiones previas requiere persistir un sessionVersion en User y contrastarlo en callbacks.jwt/session. Para rate limiting consistente en forgot-password conviene registrar intentos por email e IP en base de datos, porque PasswordResetToken por si solo no cubre emails inexistentes ni cuentas OAuth sin password."
  },
  {
    "what": "AutomatizaciÃ³n de jobs de mantenimiento",
    "why": "Centralizar cron jobs sin infraestructura nueva y evitar alertas duplicadas",
    "where": "src/app/api/cron/[job]/route.ts, src/server/jobs/*, prisma/schema.prisma",
    "learned": "Los jobs de mantenimiento funcionan mejor como un endpoint Ãºnico autenticado por CRON_SECRET y disparado por Vercel Cron. El estado de alertas se persiste en Settings.cronAlertState para notificar solo en transiciÃ³n a fallo sin crear una tabla nueva."
  },
  {
    "what": "Ciclo de vida completo del webhook de Stripe",
    "why": "Sincronizar reembolsos, fallos, expiraciones y disputas sin duplicar efectos secundarios",
    "where": "prisma/schema.prisma, src/app/api/stripe/webhook/route.ts, src/server/services/stripe-webhook-service.ts",
    "learned": "El patrÃ³n mÃ¡s estable fue separar la reconciliaciÃ³n core en base de datos de las notificaciones no crÃ­ticas: primero se consolida Payment/Appointment/CourseAccess y se registra WebhookEvent para idempotencia auditable; despuÃ©s se disparan emails/notificaciones como tareas tolerantes a fallo para no forzar reintentos de Stripe."
  }
]
